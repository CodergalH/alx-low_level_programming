#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * main - print program name followed by \n
 *
 * @argc: argument count
 * @argv: pointer to array of string of pointer
 * Return: (0) Success
 */

int main(int argc, char *argv[])
{

int count = 0;

printf("%s\n", argv[count]);

return (0);
}
