#include <stdio.h>
#include "main.h"

/**
 * mul - check if the int c is uppercase
 * @a: input
 * @b: input
 *
 * Return: 1 (success) or 0 (otherwise)
 */

int mul(int a, int b)
{
int mux;

mux = a * b;

return (mux);

}
