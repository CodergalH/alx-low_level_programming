#include <stdio.h>
#include "main.h"

/**
 * print_line - print lines equal to value of int n
 *
 * @n: interger
 */

void print_line(int n)
{
int i;

i = 0;
for (i = 0; i < n; i++)
{
if (n <= 0)
{
_putchar(10);
}
else
{
_putchar(95);
}
}
_putchar(10);

}
