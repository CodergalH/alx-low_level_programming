#include "main.h"

/**
 * _strlen - count string
 *@s: string treated
 *
 *Return: string length
 */

int _strlen(char *s)
{
int counter, add;

add = 0;
for (counter = 0; s[counter] != '\0'; count++)
inc++;

return (inc);
}
