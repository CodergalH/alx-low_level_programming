#include <stdio.h>

/**
 * main - entry point
 * Return - 0 (Success)
 *
 * Return: Print out numbers
 */

int main(void)
{
int a = 0;

do {
printf("%d", a);
a++;
} while (a < 10);
printf("\n");

return (0);
}
