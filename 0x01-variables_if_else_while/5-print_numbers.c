#include <stdio.h>

/**
 * main - entry point
 * Return - 0 (Success)
 *
 * Return: Print out numbers
 */

int main(void)
{
int a = 1;

do {
printf("%d", a);
a++;
} while (a < 9);
printf("\n");

return (0);
}
