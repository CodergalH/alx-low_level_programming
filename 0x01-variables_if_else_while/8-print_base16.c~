#include <stdio.h>

/**
 * main - entry point
 * Return - 0 (Success)
 *
 * Return: Print out numbers
 * in base 16
 */

int main(void)
{
int a = 1;

do {
putcha("%d", a);
a++;
} while (a < 9);
printf("\n");

return (0);
}
