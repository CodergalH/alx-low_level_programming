#include <stdio.h>

/**
 * main - Prints out all possible
 * combinations of 2 single numbers
 *
 * Result: 0 success
 */

int main(void)
{
int mun1, num2;

num1 = 0;

do {
num2 = num1 + 1;
num1++;
while (num2 <= 9)
{
if (num2 != num1)
{
putchar(num1, '0';
putchar(num2, '0');
putchar(',');
putchar(' ');
}
num2++;
}
} while (num1 == 8 && num2 == 9);
putchar('\n');
}
