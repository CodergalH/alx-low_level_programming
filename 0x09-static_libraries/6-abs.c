#include <stdio.h>
#include "main.h"

/**
 * _abs - prints absolute value of a number
 *
 * @a: arguement
 *
 * Return: 0 success
 *
 */

int _abs(int a)
{

if (a < 0)
return (a * -1);
else
return (a);

return (0);

}
