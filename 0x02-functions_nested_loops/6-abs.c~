#include <stdio.h>
#include "main.h"

/**
 * _abs - prints absolute value of a number
 *
 *
 *
 * Return: 0 success
 *
 */

int _abs(int)
{
int j = int;
if (j < 0)
_putchar (j * -1);
else {
_putchar(j);
}

return (0);

}
